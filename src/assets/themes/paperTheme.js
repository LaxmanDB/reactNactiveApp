import { DefaultTheme } from 'react-native-paper';

const paperTheme = {
  ...DefaultTheme,
  color: {
    ...DefaultTheme.colors,
    primary: 'cyan',
    accent: 'grey',
  },
};

export default paperTheme;
